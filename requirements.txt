# No additional dependencies required beyond ComfyUI's built-in packages
# This node uses only standard library packages:
# - urllib.request (standard library)
# - json (standard library)
# - base64 (standard library)
# - io (standard library)
#
# And packages already included with ComfyUI:
# - torch
# - numpy
# - Pillow (PIL)
